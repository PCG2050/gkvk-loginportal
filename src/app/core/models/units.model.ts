export interface Units{
    id:number;
    name:string;
    // parentUnitId:number;
    // CreatedById:number;
    // UpdatedById:number;
}
export interface unitOragnization{
    unitId:number;
    districtId:number
}